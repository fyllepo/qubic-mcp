# Qubic MCP Server Configuration
# Copy this file to .env and fill in your values.

# Network selection
# -----------------
# You can switch networks at runtime using the switch_network / add_network tools.
# Saved networks persist in ~/.qubic-mcp/networks.json.
#
# Alternatively, set QUBIC_RPC_URL here to force a specific endpoint
# (overrides the persisted active network):
# QUBIC_RPC_URL=http://192.168.1.50:21841
# QUBIC_API_URL=http://192.168.1.50:21841
# QUBIC_NETWORK_LABEL=my-local-node

# HTTP transport (optional): set a port to run as an HTTP server instead of stdio
# MCP_HTTP_PORT=3000

# SECURITY WARNING:
# NEVER put private keys or seeds in this file for production use.
# Use secure secret management (e.g., OS keychain, vault) instead.
